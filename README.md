# نظام كاشير برمجلي (Barmagly POS)

نظام إدارة نقاط البيع (POS) متكامل وشامل مصمم للمحلات التجارية، المطاعم، والشركات الصغيرة والمتوسطة. يتميز النظام بواجهة عصرية وسهلة الاستخدام، ويعمل كنسخة ويب أو تطبيق سطح مكتب (Desktop App).

---

## 🔐 بيانات الدخول الافتراضية (Default Credentials)

يأتي النظام مزوداً بحسابين افتراضيين لتسهيل التجربة:

### 1. المدير العام (Admin) - صلاحيات كاملة
*   **اسم المستخدم:** `admin`
*   **كلمة المرور:** `admin123`

### 2. الكاشير (Cashier) - صلاحيات محدودة
*   **اسم المستخدم:** `cashier`
*   **كلمة المرور:** `123456`

> **ملاحظة:** يتم إنشاء هذه الحسابات تلقائياً عند تشغيل النظام لأول مرة. يرجى تغيير كلمات المرور من صفحة "الإعدادات > المستخدمين" لضمان الأمان.

---

## 🛠️ التقنيات المستخدمة (Tech Stack)

تم بناء المشروع باستخدام أحدث تقنيات الويب لضمان السرعة والأداء العالي:

### الواجهة الأمامية (Frontend)
*   **React.js 18**: لبناء واجهة مستخدم تفاعلية وسريعة.
*   **Vite**: أداة بناء وتطوير فائقة السرعة.
*   **React Router**: للتنقل بين الصفحات بسلاسة بدون إعادة تحميل.
*   **Tailwind CSS (مخصص)**: لتصميم عصري ومتجاوب مع جميع الشاشات.
*   **Recharts**: لعرض الرسوم البيانية والإحصائيات.

### الواجهة الخلفية وقواعد البيانات (Backend & Database)
*   **Node.js & Express**: لبناء خادم API سريع وخفيف.
*   **NeDB**: قاعدة بيانات مدمجة (Embedded Database) تعمل كملفات محلية، مما يعني:
    *   **لا تحتاج لتثبيت سيرفر قواعد بيانات** (مثل SQL أو Mongo).
    *   **تعمل بدون إنترنت (Offline-First)**.
    *   سهولة النسخ الاحتياطي (مجرد نسخ لمجلد `data`).
*   **Electron**: لتحويل موقع الويب إلى برنامج سطح مكتب (Desktop App) حقيقي يعمل على ويندوز.

### الأمان (Security)
*   **BCrypt**: لتشفير كلمات المرور وحمايتها.
*   **JWT (JSON Web Tokens)**: لتأمين جلسات تسجيل الدخول.
*   **Role-Based Access Control**: نظام صلاحيات دقيق (لكل زر ووظيفة صلاحية خاصة).

---

## 📦 محتويات النظام والأقسام (Modules)

يحتوي النظام على مجموعة شاملة من الوحدات لإدارة كافة جوانب العمل:

### 1. نقطة البيع (POS)
*   واجهة بيع سريعة تدعم شاشات اللمس.
*   دعم الباركود والبحث السريع عن المنتجات.
*   تعليق الفواتير (Hold) واسترجاعها.
*   دعم طرق دفع متعددة (نقدي، فيزا، آجل، محافظ إلكترونية).
*   اختصارات لوحة المفاتيح لتسريع عملية البيع.

### 2. إدارة المخازن والمنتجات (Inventory)
*   إدارة المنتجات، الأصناف، والوحدات.
*   تتبع الكميات والتنبيه عند انخفاض المخزون.
*   إدارة المستودعات المتعددة والتحويل بينها.
*   إدارة التالف والهالك.

### 3. المشتريات والموردين (Purchases)
*   إدارة بيانات الموردين وحساباتهم.
*   إصدار فواتير الشراء وأوامر التوريد.
*   متابعة ديون الموردين ومواعيد السداد.

### 4. المبيعات والعملاء (Sales & CRM)
*   سجل كامل للمبيعات والفواتير.
*   إدارة بيانات العملاء ونظام نقاط الولاء (Loyalty Points).
*   متابعة ديون العملاء والأقساط.

### 5. الموارد البشرية (HR)
*   إدارة بيانات الموظفين والصلاحيات.
*   تسجيل الحضور والانصراف (يدوي أو بالباركود).
*   إدارة الرواتب، السلف، والمكافآت.

### 6. التقارير والتحليلات (Reports)
*   تقارير مبيعات يومية، شهرية، وسنوية.
*   تقارير الأرباح والخسائر.
*   تقارير حركة المخزون والأصناف الأكثر مبيعاً.
*   لوحة تحكم (Dashboard) تفاعلية.

### 7. الإعدادات (Settings)
*   إعدادات المتجر (الاسم، الشعار، الضريبة).
*   إدارة المستخدمين والصلاحيات.
*   النسخ الاحتياطي لقاعدة البيانات (تلقائي ويدوي).

---

## 🚀 طريقة التشغيل (How to Run)

### المتطلبات
*   Node.js (v16+)
*   Git

### خطوات التثبيت وتشغيل نسخة المطورين
1.  تحميل المكتبات:
    ```bash
    npm install
    ```
2.  تشغيل وضع الويب (للمتصفح):
    ```bash
    npm run dev
    ```
3.  تشغيل وضع سطح المكتب (Electron):
    ```bash
    npm run electron:dev
    ```

### بناء ملف التشغيل (exe)
لإخراج ملف تنفيذي للويندوز قابل للتثبيت:
```bash
npm run electron:build
```
ستجد الملف الناتج في مجلد `release/`.

---

## 📅 الخطة القادمة (Future Roadmap)

نعمل حالياً على تطوير **نظام إدارة الصيدليات (Pharmacy System)** كنسخة مخصصة من هذا النظام، وسيشمل الميزات التالية:
*   [ ] قاعدة بيانات الأدوية الجاهزة (آلاف الأصناف).
*   [ ] إدارة تواريخ الصلاحية والتنبيه قبل الانتهاء (Expiry Tracking).
*   [ ] البحث بالاسم العلمي والمادة الفعالة.
*   [ ] إدارة البدائل للأدوية الناقصة.
*   [ ] التكامل مع شركات التأمين الطبي.

---

**حقوق النشر © 2026 - شركة برمجلي**
